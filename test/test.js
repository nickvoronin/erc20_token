contract('HelloWorldToken', () => {
    it('Sets token name', () => {
        assert.equal(true, 1, "10000 wasn't in the first account");
    });
    it('sets token symbol');
    it('sets token decimals');
    it('sets total minted coins');
    it('puts minted coins to initiator balance');
});